const styles = (theme) => {
  return {
    links: {
      paddingTop: '1rem',
    },
    root: {
      flexGrow: 1,
      backgroundColor: theme.palette.background.paper,
    },
  };
};

export default styles;
